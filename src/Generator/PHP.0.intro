  if($st == "") return "";

  $length = mb_strlen($st);
  $tail = mb_substr($st, 1);
  $st1 = mb_substr($st, 0, 1);
  $st2 = "";
  $st3 = "";
  $st4 = "";
  $st5 = "";
  $st6 = "";

  if ($length >= 6) $st6 = mb_substr($st, 0, 6);
  if ($length >= 5) $st5 = mb_substr($st, 0, 5);
  if ($length >= 4) $st4 = mb_substr($st, 0, 4);
  if ($length >= 3) $st3 = mb_substr($st, 0, 3);
  if ($length >= 2) $st2 = mb_substr($st, 0, 2);
